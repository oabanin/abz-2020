
const initialState = {
	users:[],
	positions:[],
	
	links: {
	        "next_url": "",
	        "prev_url": ""
	    },
	token: "",
	fails:{}
}

const reducer = (state=initialState, action) {

	switch(action.type) {
		case "USERS_LOADED":
		return 
	}

	default:
	return state;
}